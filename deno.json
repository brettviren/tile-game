{
  "unstable": [
    "unsafe-proto",
    "sloppy-imports"
  ],
  "nodeModulesDir": "auto",
  "imports": {
    "@/": "./"
  },
  "tasks": {
    "dev": "deno run -A npm:next dev",
    "build": "NEXT_PUBLIC_BASE_PATH=$NEXT_PUBLIC_BASE_PATH deno run -A npm:next build && NEXT_PUBLIC_BASE_PATH=$NEXT_PUBLIC_BASE_PATH deno run --allow-read --allow-write --allow-env scripts/build.ts",
    "build:pages": "NEXT_PUBLIC_BASE_PATH=/tile-game deno run -A npm:next build && NEXT_PUBLIC_BASE_PATH=/tile-game deno run --allow-read --allow-write --allow-env scripts/build.ts",
    "serve": "deno run --allow-net --allow-read jsr:@std/http/file-server out/ --port 8081",
    "trials": "deno run --allow-read --allow-write --allow-env scripts/trials.ts"
  },
  "fmt": {
    "include": [
      "scripts/",
      "app/",
      "lib/"
    ]
  },
  "lint": {
    "include": [
      "scripts/",
      "app/",
      "lib/"
    ],
    "rules": {
      "exclude": [
        "no-sloppy-imports"
      ]
    }
  }
}
